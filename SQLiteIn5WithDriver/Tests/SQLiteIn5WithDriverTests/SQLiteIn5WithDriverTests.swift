import XCTest
@testable import SQLiteIn5WithDriver

final class SQLiteIn5WithDriverTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(SQLiteIn5WithDriver().text, "Hello, World!")
    }
}
